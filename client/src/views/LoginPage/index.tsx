import { Box } from "@chakra-ui/react";

import Login from "src/components/Login";

const LoginPage = () => {
  return (
    <Box padding={4}>
      <Login />
    </Box>
  );
};

export default LoginPage;
